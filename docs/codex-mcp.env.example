# Copy values from here into your shell profile or a local env file.

export GITHUB_PERSONAL_ACCESS_TOKEN=replace_with_your_github_pat

# Optional override if Node is installed outside PATH.
# export YUANLI_NODE_BIN=/absolute/path/to/node
