[project]
name = "yuanli-governance-system"
version = "0.1.0"
description = "Local canonical governance system for the Yuanli operating model."
requires-python = ">=3.9"

[tool.setuptools]
packages = ["yuanli_governance"]
